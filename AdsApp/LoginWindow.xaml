<Window x:Class="AdsApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="200" Width="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0"
                   Text="Логин:" VerticalAlignment="Center" />
        <TextBox x:Name="LoginTextBox"
                 Grid.Row="0" Grid.Column="1" Margin="5,5,0,5" />

        <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="Пароль:" VerticalAlignment="Center" />
        <PasswordBox x:Name="PasswordBox"
                     Grid.Row="1" Grid.Column="1" Margin="5,5,0,5" />

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Войти" Width="80" Margin="0,10,5,0"
                    Click="LoginButton_Click" />
            <Button Content="Отмена" Width="80" Margin="0,10,0,0"
                    Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
