<Window x:Class="AdsApp.ProfitInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Прибыль" Height="150" Width="300"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0"
                   Text="Сумма прибыли:" VerticalAlignment="Center"/>
        <TextBox x:Name="ProfitTextBox"
                 Grid.Row="0" Grid.Column="1" Margin="5,0,0,0"/>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="0,10,5,0"
                    Click="OkButton_Click"/>
            <Button Content="Отмена" Width="80" Margin="0,10,0,0"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
